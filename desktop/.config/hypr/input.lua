-- Personal input overrides migrated from input.conf.
hl.config({
  input = {
    kb_layout = "us",
    kb_variant = "altgr-intl",
    kb_options = "caps:capslock",
    repeat_rate = 40,
    repeat_delay = 600,
    numlock_by_default = true,

    touchpad = {
      scroll_factor = 0.4,
    },

    tablet = {
      output = "DP-1",
    },
  },
})

-- Terminal touchpad rules are already supplied by Omarchy's defaults.
